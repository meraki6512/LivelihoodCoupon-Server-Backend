kakao:
  api:
    key: ${KAKAO_API_KEY}
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/livelihoodCoupon_db?sslmode=disable&connectTimeout=10&options=-c TimeZone%3DAsia/Seoul
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update